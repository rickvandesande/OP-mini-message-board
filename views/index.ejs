<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages</title>
</head>
<body>
    <% posts.rows.forEach(message => { %>
       <div class="message-container">
            <i>On <%= message.added %> <%= message.username%> wrote:</i>
            <p><%= message.text %></p>
       </div> 
    <% }); %>
</body>
</html>